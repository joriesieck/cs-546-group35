// import user data
const users = require('./users');
const questions = require('./questions');
const ratings = require('./ratings');

// export data
module.exports = {
    users,
    questions,
    ratings
};
